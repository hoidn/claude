 read dev guide, test practices doc(s), and architecture md
---
 run pytest tests/ WITHOUT timeout.  review the output. use parallel
      / concurrent subagents to explore relevant proj docs and source files and decide, for each failure, whether the test is deprecated or the
   impl is
      buggy. then analyze subagent findings and think about a fix approach. then use parallel / concurrent subagents to fix the failures

